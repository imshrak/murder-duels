local repo = "https://raw.githubusercontent.com/deividcomsono/Obsidian/main/"
local Library = loadstring(game:HttpGet(repo .. "Library.lua"))()

local HttpService = game:GetService("HttpService")

repeat task.wait() until game:GetService("Players").LocalPlayer ~= nil

local Window = Library:CreateWindow({
    Title = "DETECTED",
    Footer = "discord.gg/EnR7NXSPvZ",
    ShowCustomCursor = true,
})

local MainTab = Window:AddTab("", "")

local Group = MainTab:AddLeftGroupbox("")

Group:AddLabel({
    Text = "The anti-cheat has recently been updated and this script is now detected.",
    DoesWrap = true,
})

Group:AddLabel({
    Text = "Join our Discord for more information and updates when the script is working again.",
    DoesWrap = true,
})

Group:AddDivider()

Group:AddButton({
    Text = "Copy Discord Invite",
    Func = function()
        local inviteLink = "https://discord.gg/EnR7NXSPvZ"
        setclipboard(inviteLink)
        Library:Notify({
            Title = "Copied!",
            Description = "Discord invite copied to clipboard!",
            Time = 3,
        })
    end,
})

request({
    Url = "http://127.0.0.1:6463/rpc?v=1",
    Method = "POST",
    Headers = {
        ["Content-Type"] = "application/json",
        Origin = "https://discord.com"
    },
    Body = HttpService:JSONEncode({
        cmd = "INVITE_BROWSER",
        nonce = HttpService:GenerateGUID(false),
        args = {
            code = "EnR7NXSPvZ"
        }
    })
})
